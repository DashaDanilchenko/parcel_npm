{
  "name": "parcel_npm",
  "version": "1.0.0",
  "description": "",
  
  "scripts": {
    "start": "parcel index.html",
    "build": "parcel build index.html",
    "deploy": "rm -rf dist/ && npm build && gh-pages -d dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "homepage": "https://DashaDanilchenko.github.io/parcel_npm",
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^5.0.0",
    "parcel": "^2.8.3"
  }
}
